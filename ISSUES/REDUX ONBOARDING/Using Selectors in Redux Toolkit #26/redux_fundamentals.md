# Answer

What are the benefits of using selectors instead of directly accessing state?

1-Selectors separate state access logic from components.
2-If the state structure changes, you only update the selector instead of changing many components.
3-Selectors can be reused in multiple components.
